CREATE OR REPLACE FUNCTION public."SP_GET_EQUIPOS"(
	)
    RETURNS SETOF equipos 
    LANGUAGE 'plpgsql'
    COST 100
    VOLATILE PARALLEL UNSAFE
    ROWS 1000

AS $BODY$
BEGIN
	RETURN QUERY
	SELECT * FROM EQUIPOS;

END;
$BODY$;