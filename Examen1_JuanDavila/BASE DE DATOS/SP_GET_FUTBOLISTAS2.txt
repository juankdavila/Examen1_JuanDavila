CREATE OR REPLACE FUNCTION public."SP_GET_FUTBOLISTAS"(
	)
    RETURNS SETOF futbolista 
    LANGUAGE 'plpgsql'
    COST 100
    VOLATILE PARALLEL UNSAFE
    ROWS 1000

AS $BODY$
BEGIN
	RETURN QUERY
	SELECT * FROM FUTBOLISTA;

END;
$BODY$;
